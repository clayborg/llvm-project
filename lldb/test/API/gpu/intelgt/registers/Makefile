# IntelGT SYCL test Makefile
# Uses LLDB's standard Makefile.rules infrastructure

SYCL_SOURCES := simple_kernel.cpp

# Output binary name (without extension)
EXE := simple_kernel

# Include standard LLDB test Makefile rules
include Makefile.rules
